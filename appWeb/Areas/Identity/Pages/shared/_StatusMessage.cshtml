@model string

@if (!string.IsNullOrEmpty(Model))
{
    var statusMessageClass = Model.StartsWith("Error") ? "danger" :
                             Model.StartsWith("Success") ? "success" :
                             "info";

    <div class="alert alert-@statusMessageClass" role="alert">
        @Model
    </div>
}
